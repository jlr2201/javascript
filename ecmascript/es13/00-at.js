// acceder al arreglo

const array = ['one', 'two', 'three', 'four', 'five', 'six'];

console.log(array[array.length - 1]); // busco el último elemento del arreglo

console.log(array.at(-1)); // lo mismo con es13

