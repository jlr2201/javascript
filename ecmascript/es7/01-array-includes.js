// Permite trabajar sobre un arreglo y permite saber si se encuentra un elemento

let numbers = [1, 3, 4, 6, 7, 8,];

console.log(numbers.includes(4));

const list = ['oscar', 'David', 'Ana'];
console.log(list.includes('Oscar'));


// Continuar en cap. 19 